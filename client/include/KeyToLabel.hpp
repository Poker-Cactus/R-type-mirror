/**
 * @file KeyToLabel.hpp
 * @brief Keyboard key code to label conversion
 */

#pragma once

#include <string>

/**
 * @brief Convert a key code to human-readable label
 * @param key Key code value
 * @return String representation of the key
 */
std::string KeyToLabel(int key);
